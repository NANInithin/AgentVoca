"""Observer session compilers (v0.4.0).

Built-ins: ``rules`` (zero-config default), ``openai_compatible`` (LLM),
``none`` (raw dump). Registered as ``"module:Class"`` dotted paths in
``core/registry.py`` so they are imported lazily (R14).
"""
